# counter_app (Flutter client)

The Flutter client for the next-dart counter demo. It renders a server-driven UI
fetched from the demo backend via `RfwRenderer`.

See [`../README.md`](../README.md) for how to run the full demo (backend + app).
