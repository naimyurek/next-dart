name: app
description: "Flutter demo app for next-dart server-driven UI."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.12.0

dependencies:
  flutter:
    sdk: flutter
  next_dart_client:
    path: ../../../packages/next_dart_client
  next_dart_rfw:
    path: ../../../packages/next_dart_rfw
  cryptography: ^2.9.0
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  counter_server:
    path: ../server
  http: ^1.2.0
  shelf: ^1.4.2

flutter:
  uses-material-design: true
